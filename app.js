var todoApp = angular.module('todo', ['ngResources', 'ngMockE2E']);

weatherApp.config(function($routeProvider){
	
	$routeProvider

	.when('/', {
		templateUrl:
	})

	.when('/todos', {
		templateUrl: 
	})
})